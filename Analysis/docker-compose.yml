version : '2'


services:
    analytics:
      image: madderle/twitter-sentiment-analysis:analysis-1.0
      environment:
        - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
        - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
        - REDIS_IP = ${REDIS_IP}
